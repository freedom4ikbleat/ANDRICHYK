#include <iostream>
#include <math.h>

using namespace std;

int main()
{
    int a = 4, b = 5, c, d, e;
    c = pow(a, 2) + pow(b, 2);
    cout << c << endl;
    d = a * b / 2;
    cout << d << endl;
    cin >> e;
}
