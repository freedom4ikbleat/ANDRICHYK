#include <iostream>

using namespace std;


int main()
{
    int x, y, c;
    cout << "Vvedite 2 chisla" << endl;
    cin >> x;
    cin >> y;
    cout << (abs(x) - abs(y)) / 1 + abs(x * y) << endl;
    cin >> c;
}
